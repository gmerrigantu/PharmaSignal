-r requirements.txt
boto3>=1.34
pytest>=8.0
pytest-cov>=5.0
# Spark backfill jobs run locally for dev/test; EMR Serverless provides Spark in cloud.
pyspark>=3.5,<3.6
venv-pack>=0.2          # packages the deps venv for EMR Serverless (spark/build_deps.sh)
