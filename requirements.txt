# PharmaSignal runtime dependencies (pinned loosely for reproducibility).
# Core lakehouse + analytics
pandas>=2.0
pyarrow>=14.0          # Parquet read/write
duckdb>=0.10           # query-on-demand over Parquet (local lakehouse)
fsspec>=2024.0         # unified local/S3 filesystem access
numpy>=1.26
PyYAML>=6.0
requests>=2.31

# Dashboard
streamlit>=1.40
altair>=5.0            # bubble / CI / trend charts

# NHANES XPT parsing (pandas.read_sas backend) + survey stats
# (pandas reads .xpt natively; no extra dep required for MVP)

# Dev / test (install with: pip install -r requirements-dev.txt)
